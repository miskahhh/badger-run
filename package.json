{
    "name": "badger-run",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "description": "Badger Run — a 3D endless runner built with THREE.js for CS559.",
    "scripts": {
      "dev": "vite",
      "build": "vite build",
      "preview": "vite preview",
      "predeploy": "npm run build",
      "deploy": "gh-pages -d dist"
    },
    "dependencies": {
      "three": "^0.170.0"
    },
    "devDependencies": {
      "gh-pages": "^6.2.0",
      "vite": "^5.4.0"
    }
  }